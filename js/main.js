// Ponto de entrada JavaScript para inicialização dos módulos do portfólio

document.addEventListener('DOMContentLoaded', () => {
  // Inicialização confirmada
  console.log('Portfólio João Kennedy carregado com sucesso.');
});